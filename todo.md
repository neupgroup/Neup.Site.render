make a webhook.
it will be called as:

/bridge/webhook.v1/syncer

in here ton.he customers will be passing in multiple types of informati

Typecheck fails because `core/utils.ts` imports missing dependencies: `clsx` and `tailwind-merge`.

`npm install` reports 16 dependency vulnerabilities: 1 low, 3 moderate, and 12 high.

Typecheck fails because `.next/types/validator.ts` references missing `../../app/assets/[asset]/route.js` declarations.

`npm install` reports pending install-script approval for `@prisma/engines`, `esbuild`, `prisma`, `sharp`, and `unrs-resolver`.
